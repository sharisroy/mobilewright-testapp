name: Mobile Tests

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

env:
  MOBILEWRIGHT_DISABLE_TELEMETRY: "1"
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

# Allow the deploy job to publish the HTML report to GitHub Pages.
permissions:
  contents: read
  pages: write
  id-token: write

# Don't let overlapping runs clobber an in-progress Pages deploy.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  android-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Install dependencies
        run: npm ci

      - name: Run @smoke on emulator
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 33
          arch: x86_64
          target: default
          profile: pixel_6
          force-avd-creation: true
          emulator-boot-timeout: 900
          emulator-options: >-
            -no-snapshot-save
            -no-window
            -gpu swiftshader_indirect
            -noaudio
            -no-boot-anim
            -camera-back none
            -memory 4096
            -no-metrics
          disable-animations: true
          script: |
            set -e

            export ANDROID_HOME="${ANDROID_HOME:-$ANDROID_SDK_ROOT}"
            export PATH="$ANDROID_HOME/platform-tools:$ANDROID_HOME/emulator:$PATH"
            export MW_DEVICE_ID="test"
            unset ANDROID_SERIAL

            echo "=== Waiting for emulator to fully boot ==="
            adb wait-for-device
            adb shell 'while [ "$(getprop sys.boot_completed)" != "1" ]; do sleep 2; done'
            adb shell 'while [ "$(getprop dev.bootcomplete)" != "1" ]; do sleep 2; done'
            adb shell 'while [ "$(getprop init.svc.bootanim)" != "stopped" ]; do sleep 2; done'
            echo "=== Emulator fully booted ==="

            adb shell input keyevent 82
            adb shell input keyevent 66
            sleep 2

            echo "=== Installing APK ==="
            adb install -r ./APPs/app-debug.apk

            echo "=== Installing mobilewright agent ==="
            npx mobilewright install --device test

            echo "=== Running @gitAction tests ==="
            # --retries 2: gesture tests (long-press, swipe) are timing-sensitive
            # on swiftshader emulators — retry gives them a fair chance before failing.
            # --grep "@gitAction": run every test tagged @gitAction across the suite.
            MW_DEVICE_ID=test npx mobilewright test --grep "@gitAction" --retries 2 --reporter html

      - name: Upload HTML report (downloadable artifact)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mobilewright-report
          path: mobilewright-report
          if-no-files-found: ignore

      - name: Upload report as Pages artifact
        if: always()
        uses: actions/upload-pages-artifact@v3
        with:
          path: mobilewright-report

  # Publish the HTML report to GitHub Pages on tag pushes and on manual
  # workflow_dispatch runs.
  deploy-report:
    needs: android-tests
    if: always() && (startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch')
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
